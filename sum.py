def add(a, b) -> c:
    """
    Add two python objects (if summable)
    Use as:
    sum_int = sum(1, 6)
    or:
    long_string = sum("hello", " world")
    another example:
    sum_float = sum(1,10)
    """
    return a + b
